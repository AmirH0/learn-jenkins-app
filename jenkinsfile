pipeline {
    agent any
    stages {
        stage('build') {
            agent {
                docker {
                    image 'node:alpine'
                    
                }
            }
            steps {
                sh '''
                    
                    node --version
                    npm --version
                    npm install
                    npm run build
                    ls -la
                '''
            }
        }
    }
}