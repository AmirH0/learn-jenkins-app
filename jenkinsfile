pipeline {
    agent any
    stages {
        stage('build') {
            agent {
                docker {
                    image 'node:alpine'
                    
                }
            }
            steps {
                sh '''
                    
                    node --version
                    npm --version
                    chown -R 122:124 /root/.npm
                    npm ci
                    npm run build
                    ls -la
                '''
            }
        }
    }
}